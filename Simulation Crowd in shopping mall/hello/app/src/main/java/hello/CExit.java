package hello;

import javax.swing.*;
import java.awt.*;

public class CExit extends CObj {
	public CExit(int W, int K) {
		super(W, K);
		typ = 'O';
		img = new ImageIcon("imgO.png").getImage();

	}
}
